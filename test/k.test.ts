test('Should filter outages that begins with some date', () => {
    expect(1 + 3).toBe(4);
});

test('Should filter outages that does not have its id in site-information', () => {
    expect(1 + 3).toBe(4);
});

